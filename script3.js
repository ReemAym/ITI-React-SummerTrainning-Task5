var r=prompt("enter circle's radius ","Type Radius here");
var area=Math.PI*r*r;

alert("Total area of acircle is "+area);


var x=prompt("What ia the value you want to calculate it's square root","Type value here");
var y=Math.sqrt(x);

alert("square root for "+x+" = "+y);

var angle=prompt("what is the value you want to calculate cos for ","Type angle here");
 var rad=(Math.PI/180)*angle;

 document.write("cos "+angle+"<span>&deg</span> is "+Math.cos(rad));